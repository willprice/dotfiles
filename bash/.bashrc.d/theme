powerline-daemon -q
POWERLINE_BASH_CONTINUATION=1
POWERLINE_BASH_SELECT=1
POWERLINE_DIR="$(python3 -c 'from pathlib import Path; import powerline; print(Path(powerline.__file__).parent)')"
. "$POWERLINE_DIR/bindings/bash/powerline.sh"

# vim: set ft=sh:
