# vim: set ft=sh:

alias pacman='sudo pacman --color=auto'
alias pacaur='sudo pacaur'
alias ls='ls --color'
alias g=git
alias tmux='tmux -u'
if command -v trash-put >/dev/null 2>&1; then
    alias rm='echo "Use trash-put instead of rm"'
fi
