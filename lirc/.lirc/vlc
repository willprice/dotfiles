begin
    remote = mceusb
    prog = vlc
    button = KEY_PAUSE
    config = key-pause
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_PLAY
    config = key-play
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_REWIND
    config = key-slower
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_FORWARD
    config = key-faster
    repeat = 0
    delay = 0
end


begin
    remote = mceusb
    prog = vlc
    button = KEY_VOLUMEDOWN
    config = key-vol-down
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_VOLUMEUP
    config = key-vol-up
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_MUTE
    config = key-vol-mute
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = vlc
    button = KEY_ENTER
    config = key-nav-activate
    repeat = 0
    delay = 0
end

